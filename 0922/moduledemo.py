import math

def hap(x, y) :
    return x + y

def gop(x, y) :
    return x *y

PI = 3.141592

def mycalc(myvalue) :
    print(f"floor = {math.ceil(myvalue)}")
    print(f"ceil = {math.ceil(myvalue)}")
    print(f"round = {round(myvalue)}")
